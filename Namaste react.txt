
passing a prop is just passing arugument to the component 
just like function call (arg)

dynamically passing means through props we can pass

configdrivenUI-our website is built by data (controlling ur data using config )(suppose if we are in bengalore then the data shown in bangalore is different (restaurent) likewise
if we are in other state it will bring the data according to the state  )


optional chaining ----homework

map,list,reduce--aksay saini yt 

need to pass key on all the map

react official document says that in map dont use index as key

see the document for index